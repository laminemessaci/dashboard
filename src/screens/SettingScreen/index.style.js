const styles = {
  container: (theme) => ({
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    marginLeft: 12,
  }),

  centered: (theme) => ({
    flex: 1,
    justifyContent: "center",
  }),


};

export default styles;
