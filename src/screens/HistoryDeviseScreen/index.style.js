const styles = {
  container: (theme) => ({
    flex: 1,
    justifyContent: "center",
    marginLeft: 16,
  }),

  centered: (theme) => ({
    flex: 1,
    justifyContent: "center",
    marginTop: 120,
  }),
  description: (theme) => ({
    marginLeft: 24,
    marginTop: 12,
    marginBottom: 12,
  }),
};

export default styles;
