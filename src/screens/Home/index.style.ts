const styles = {
container: (theme) => ({
  flex: 1,
  justifyContent: "center",
  marginLeft: 12,
  

}),

  centered: (theme) => ({
    flex: 1,
    justifyContent: "center",
  }),

  description: (theme)=>({
    marginLeft: 24,
    marginTop: 12,
    marginBottom: 12
  })
};

export default styles;
