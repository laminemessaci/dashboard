import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const CalculIMMC = ({name, size, weight}) => {
  return (
    <View>
      <Text>CalculIMMC is : -----</Text>
    </View>
  )
}

export default CalculIMMC

const styles = StyleSheet.create({})