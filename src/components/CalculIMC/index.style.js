const styles = {
  container: (theme) => ({
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    marginLeft: 12,
  }),

  centered: (theme) => ({
    flex: 1,
    justifyContent: "center",
  }),

  image: (theme) => ({
    width: 60,
    height: 60,
    marginLeft: 24,
    marginTop: 12,
    marginBottom: 12,
  }),
};

export default styles;
